# 版本要点

本项目的成品是单文件离线 HTML，版本号体现在文件名与文档里。
早期迭代没有逐版打标签，下面按能力演进列出要点；构建产物见 Releases。

## v2.8 — 2026-09-10

- **材料包导出**：按 `学号姓名/五育测评/+分值 (事由)` 打包命中名单原件，可直接交班长
  - 命名规则：原文件名保持不变，只在最前面加分值（`+2 晚归打卡公示汇总.pdf`）
  - 没识别到分值的放 `未识别/`，并加 `未识别分值 ` 前缀
  - 只记录来源引用不复制字节，导出前估算体积，超 300 MB 直接提示
- **检索明细重排**：台账 + 明细两级结构，`学号 · 姓名 → 五育测评 → +分值（事由）`
- **隐私加固**：示例姓名全部匿名化；新增发布门禁 `publish_check.py`
- 构建脚本锚点改为可被独立复算的形式，基线可对外分发

## v2.7

- 明细清单改为按「人」分块，五育顺序固定为 德 → 智 → 体 → 美 → 劳
- 德育超 25、智育超 10 时在对应育别直接标注「超出上限，按上限计」
- 台账列数随「有记录的育」变化，缺项显式点名，不再用固定三列掩盖体育等育别

## v2.6

- 进度条与倒计时重写：百分比、已用时间、预计剩余时间逐帧一致
- 移除进度条过渡动画，避免宽度与数字不同步
- 状态行分隔符与后一字段绑定换行，不再出现行尾孤零零的「·」

## v2.5

- 前端 UI 重构：纸张质感配色、五育专属色、卡片化分区、拖放区即文件选择器
- 无障碍：键盘可达、焦点可见、`role=progressbar` 语义、reduced-motion 适配
- 配色按 WCAG 2.1 AA 逐项校验

## v2.4

- 引入 PP-OCRv4 移动端 WebGPU 引擎，扫描件识别显著提速
- GPU 不可用时自动回退内置 Tesseract.js
- 解析结果缓存到 IndexedDB，重复查询不重复 OCR

## v2.3 及更早

- 单文件离线版成型：zip / PDF / Excel 解析、姓名检索、去重、Excel 导出
- 逐步补齐模糊匹配、同名不同人提醒、大文件流式处理
