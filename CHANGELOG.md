# 版本要点

本项目的成品是单文件离线 HTML，版本号体现在 Release 标签上（文件名保持稳定）。
早期迭代没有逐版打标签，下面按能力演进列出要点。

## v2.9 — 2026-09-10

**成品 HTML 未变**（sha256 仍是 `0e7c105baca9b3702e008ed8c06e573e40eed913e5d29f53e4a1cd840440dc24`），
本版只修启动器。

- **本地服务不再静默常驻**：v2.8 的 `启动本地服务.cmd` 用 `start /min` 把服务甩到一个最小化窗口，
  启动器随即退出，那句「关掉最小化窗口可停服务」的提示也跟着消失——用户根本看不到，
  于是 python 一直留在后台。
  - 现在服务**就在启动器窗口里前台运行**：关掉窗口（或 Ctrl+C）即停止服务，
    已打开的页面照常使用（单文件自包含，加载后不再请求服务）。
- **不再依赖 Python**：服务统一由 Windows 自带的 PowerShell 承担（`本地服务_无Python.ps1`），
  因此不会再启动任何 python 进程；有没有装 Python 体验一致。
- **服务脚本加固**：加 5 秒读超时，避免浏览器先开一条空闲连接却不发请求时，
  单线程服务器被卡住导致页面请求等不到响应（实测：空闲连接只让请求延迟约 5 秒，不会永久卡死）。

## v2.8 — 2026-09-10

**发布**：[v2.8](https://github.com/Bonger34/score-ocr-tool/releases/tag/v2.8)
· 附件 `score-ocr-tool.zip`（成品 + 3 个启动器）
· 成品 sha256 `0e7c105baca9b3702e008ed8c06e573e40eed913e5d29f53e4a1cd840440dc24`

- **材料包导出**：按 `学号姓名/五育测评/+分值 (事由)` 打包命中名单原件，可直接交班长
  - 命名规则：原文件名保持不变，只在最前面加分值（`+2 晚归打卡公示汇总.pdf`）
  - 没识别到分值的放 `未识别/`，并加 `未识别分值 ` 前缀
  - 只记录来源引用不复制字节，导出前估算体积，超 300 MB 直接提示
- **检索明细重排**：台账 + 明细两级结构，`学号 · 姓名 → 五育测评 → +分值（事由）`
- **隐私加固**：示例姓名全部匿名化；新增发布门禁 `publish_check.py`
- **仓库自足**：42MB 构建底座 `.tools/v24_ui_baseline.html` 随仓库提供，
  `git clone` 后直接 `build_v25_ui.py` 即可复现成品，无需额外下载
- **启动器更名**：`启动本地服务.cmd` / `直接打开页面.cmd` / `本地服务_无Python.ps1`；
  顺带修好「无 Python 兜底脚本因缺 UTF-8 BOM 而根本无法运行」的老问题，
  并新增 `launcher_check.py` 防止回归

## v2.7

- 明细清单改为按「人」分块，五育顺序固定为 德 → 智 → 体 → 美 → 劳
- 德育超 25、智育超 10 时在对应育别直接标注「超出上限，按上限计」
- 台账列数随「有记录的育」变化，缺项显式点名，不再用固定三列掩盖体育等育别

## v2.6

- 进度条与倒计时重写：百分比、已用时间、预计剩余时间逐帧一致
- 移除进度条过渡动画，避免宽度与数字不同步
- 状态行分隔符与后一字段绑定换行，不再出现行尾孤零零的「·」

## v2.5

- 前端 UI 重构：纸张质感配色、五育专属色、卡片化分区、拖放区即文件选择器
- 无障碍：键盘可达、焦点可见、`role=progressbar` 语义、reduced-motion 适配
- 配色按 WCAG 2.1 AA 逐项校验

## v2.4

- 引入 PP-OCRv4 移动端 WebGPU 引擎，扫描件识别显著提速
- GPU 不可用时自动回退内置 Tesseract.js
- 解析结果缓存到 IndexedDB，重复查询不重复 OCR

## v2.3 及更早

- 单文件离线版成型：zip / PDF / Excel 解析、姓名检索、去重、Excel 导出
- 逐步补齐模糊匹配、同名不同人提醒、大文件流式处理
